<!DOCTYPE html>
<html>
<head>{>"shared/head"/}
</head>
<body>
	{>"shared/nav"/}

	<div class="container">
		<div class="page-header">
			<h1>{site.title}</h1>
		</div>
		<p class="lead" id="intro">{site.intro}</p>

		<ul class="list-group">

			{#userList}
			<a href="/user/view/{username}" class="list-group-item">{username}</a>
			{~n} {/userList}

		</ul>
	</div>

	{>"shared/footer"/}
</body>
</html>